<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.mapper.RoleMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO role (name, code)
        VALUES (#{name}, #{code})
    </insert>

    <select id="selectAllWithPage" resultType="com.mall.po.entity.Role">
        SELECT *
        FROM role
        ORDER BY id DESC
            LIMIT #{offset}, #{pageSize}
    </select>

    <select id="countAll" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM role
    </select>

    <select id="searchByNameWithPage" resultType="com.mall.po.entity.Role">
        SELECT *
        FROM role
        WHERE name LIKE CONCAT('%', #{name}, '%')
        ORDER BY id DESC
            LIMIT #{offset}, #{pageSize}
    </select>

    <select id="countByName" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM role WHERE name LIKE CONCAT('%', #{name}, '%')
    </select>
</mapper>

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.mapper.ProductMapper">

    <select id="selectById" resultType="com.mall.entity.Product">
        SELECT *
        FROM product
        WHERE id = #{id}
    </select>

    <select id="selectAll" resultType="com.mall.entity.Product">
        SELECT *
        FROM product
    </select>

    <update id="updateStock">
        UPDATE product
        SET stock = #{stock}
        WHERE id = #{id}
    </update>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product (name, price, stock,description)
        VALUES (#{name}, #{price}, #{stock},#{description})
    </insert>

    <select id="searchByName" resultType="com.mall.entity.Product">
        SELECT *
        FROM product
        WHERE name like CONCAT('%', #{name}, '%')
    </select>
</mapper>
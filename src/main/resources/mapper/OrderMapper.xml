<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.mapper.OrderMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO orders(user_id, status, total_amount, create_time)
        VALUES (#{userId}, #{status}, #{totalAmount}, #{createTime})
    </insert>

    <update id="closeTimeoutOrders">
        UPDATE orders
        SET status = 3
        WHERE status = 0
          AND create_time &lt; NOW() - INTERVAL 30 MINUTE
    </update>

    <update id="update">
        UPDATE orders
        SET status = #{status},
            total_amount = #{totalAmount},
            create_time = #{createTime}
        WHERE id = #{id}
    </update>


    <select id="selectByUserId" resultType="com.mall.po.entity.Order">
        SELECT * FROM orders WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>

    <select id="selectById" resultType="com.mall.po.entity.Order">
        SELECT * FROM orders WHERE id = #{orderId}
    </select>

    <select id="selectByUserIdWithPage" resultType="com.mall.po.entity.Order">
        SELECT * FROM orders WHERE user_id = #{userId} ORDER BY create_time DESC LIMIT #{offset}, #{pageSize}
    </select>

    <select id="countByUserId" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM orders WHERE user_id = #{userId}
    </select>

</mapper>

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.mapper.OrderMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO orders(user_id, status, total_amount,create_time)
        VALUES (#{userId}, #{status},#{totalAmount}, #{createTime})
    </insert>

    <update id="closeTimeoutOrders">
        UPDATE orders
        SET status = 3
        WHERE status = 0
        AND create_time &lt; NOW() - INTERVAL 30 MINUTE
    </update>

</mapper>
